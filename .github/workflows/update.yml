name: Update
on:
  schedule:
    - cron: 0 18 * * *
  workflow_dispatch:

jobs:
  update-flake:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Install nix
        uses: DeterminateSystems/nix-installer-action@main
        with:
          extra-conf: |
            extra-substituters = https://cache.garnix.io
            extra-trusted-public-keys = cache.garnix.io:CTFPyKSLcx5RMJKfLo5EEPUObbA78b0YQ2DTCJXqr9g=

      - name: Use nix cache
        uses: DeterminateSystems/flakehub-cache-action@main

      - name: Compare nixpkgs version
        id: compare
        run: |
          current_rev=$(nix flake metadata --json | jq -r .locks.nodes.nixpkgs.locked.rev)
          latest_rev=$(nix flake metadata github:NixOS/nixpkgs/nixos-unstable --json | jq -r .revision)

          echo "Current revision: $current_rev"
          echo "Latest revision: $latest_rev"

          if [ "$current_rev" = "$latest_rev" ]; then
            echo "upgrade=false" >>"$GITHUB_OUTPUT"
          else
            echo "upgrade=true" >>"$GITHUB_OUTPUT"
          fi

      - name: Update flake
        uses: DeterminateSystems/update-flake-lock@main
        if: steps.compare.outputs.upgrade == 'true'
        with:
          commit-msg: "bump: flake inputs"
          branch: bump_flake_inputs
          pr-title: "bump: flake inputs"
          pr-body: |
            Automated changes by GitHub Action.

            ```
            {{ env.GIT_COMMIT_MESSAGE }}
            ```
          pr-labels: merge-queue
